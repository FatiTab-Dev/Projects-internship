name: Deploy Frontend to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: Task1-CodeAlpha_Simple_Ecommerce_Store/Front-end/package-lock.json

      - name: Install dependencies
        working-directory: Task1-CodeAlpha_Simple_Ecommerce_Store/Front-end
        run: npm ci

      - name: Build
        working-directory: Task1-CodeAlpha_Simple_Ecommerce_Store/Front-end
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: Task1-CodeAlpha_Simple_Ecommerce_Store/Front-end/dist

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
